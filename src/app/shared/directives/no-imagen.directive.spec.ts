import { NoImagenDirective } from './no-imagen.directive';

describe('NoImagenDirective', () => {
  it('should create an instance', () => {
    const directive = new NoImagenDirective();
    expect(directive).toBeTruthy();
  });
});
