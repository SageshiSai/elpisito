import { MetrosCuadradosPipe } from './metros-cuadrados.pipe';

describe('MetrosCuadradosPipe', () => {
  it('create an instance', () => {
    const pipe = new MetrosCuadradosPipe();
    expect(pipe).toBeTruthy();
  });
});
