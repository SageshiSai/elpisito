import { NumeroImagenesPipe } from './numero-imagenes.pipe';

describe('NumeroImagenesPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroImagenesPipe();
    expect(pipe).toBeTruthy();
  });
});
